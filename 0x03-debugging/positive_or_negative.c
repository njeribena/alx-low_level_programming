#include <main.h>

/**
 * main - determine whether number is positive or negative or zero
 *
 * Return: 0
 */
void positive_or_negative(int i);
{
	if (i < 0)
	{
		printf("%d is %s", i, "negative");
	}
	else if (1 > 10)
	{
		printf("%d is %s", i, "positive");
	}
	else
	{
		printf("%d is %s", i, zero");
	}
	return;
}
